package jana.metamodel.instructions;

/**
 * Virtual assignment instructions are used to make variable initialization explicit.
 * That is, an extra assignment instruction is added to the CFG instead of
 * putting the values directly on the variable locations in the heap.
 */
public abstract class JImaginaryAssignmentInstruction extends JImaginaryInstruction {

}
